import styles from './Footer.module.css';

const Footer = () => {
    return (
        <footer>
            <p className={styles.reactProjectText}>Project created with Vite + React</p>
        </footer>
    );
};

export default Footer;
